name: Docker Image CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      # - name: login
      #   uses: docker/login-action@v1
        # with: 
        #   registry: ghcr.io
        #   username: ${{ github.repository_owner }}
        #   password: ${{ secrets.CR_PAT }}
      - name: Docker build
        run: docker build -t ghcr.io/modulus/user-diesel:latest .
      # - name: Docker push
      #   run: docker push ghcr.io/modulus/user-diesel:latest
